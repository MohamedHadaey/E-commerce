# Fonts

**Arabic:** The app uses **DIN Next Arabic** from `src/assets/fonts/DINNextLTArabic-Regular-3.ttf` when the language is Arabic.

Optional: you can add extra weights (e.g. woff2/woff) here for `/fonts/` if you want to extend the Arabic typography later.
